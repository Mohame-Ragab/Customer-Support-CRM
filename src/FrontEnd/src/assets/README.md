# Assets

Static images/icons/fonts imported by components go here. Empty for now — the
only current static asset (the favicon) lives in `public/`, which is served
as-is rather than processed by the bundler.
