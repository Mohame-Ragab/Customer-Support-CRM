# Utils

Pure, framework-agnostic helper functions with no feature ownership (no React,
no API calls, no side effects) go here. Empty for now - nothing needed one yet
that doesn't already belong in `lib/` (which owns cross-feature
_infrastructure_, as opposed to plain pure functions).
