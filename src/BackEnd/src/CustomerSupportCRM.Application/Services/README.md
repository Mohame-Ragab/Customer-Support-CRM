# Services

Application services implement use cases/orchestration that don't cleanly belong
to a single feature folder yet (e.g. cross-cutting workflows). Prefer placing
feature-specific logic under `Features/<Feature>/` first. Empty for now.
