# Mappings

AutoMapper `Profile` classes go here (or alongside their feature under
`Features/<Feature>/`). Auto-registered by `DependencyInjection.AddApplication()`
via assembly scanning. Empty for now — no mappings exist until entities/DTOs do.
