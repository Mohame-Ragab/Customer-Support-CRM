# Entities

CRM aggregate/entity classes (e.g. `Customer`, `Ticket`, `Agent`) are added here,
each inheriting `Common.BaseEntity`. Not implemented yet — this architecture
change intentionally contains no business entities.
