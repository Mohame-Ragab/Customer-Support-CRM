namespace CustomerSupportCRM.Domain.Enums;

/// <summary>Fixed priority levels for a ticket (tickets/set-ticket-category-and-priority).</summary>
public enum TicketPriority
{
    Low = 1,
    Medium = 2,
    High = 3,
    Urgent = 4,
}
