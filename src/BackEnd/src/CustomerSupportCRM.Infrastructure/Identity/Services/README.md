# Identity Services

Reserved for an `IIdentityService` implementation (wrapping `UserManager`,
`RoleManager`, `SignInManager`) once authentication use-cases (login, register,
refresh token, password reset, etc.) are implemented. Not created yet — no such
operations exist in this architecture change (see docs/architecture.md,
"Authentication architecture").
